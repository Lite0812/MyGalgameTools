# =============================================================================
# Siglus G00 Toolkit 依赖列表
# =============================================================================
# 安装命令:
#   基础:    pip install -r requirements.txt
#   完整:    pip install -r requirements.txt -r requirements-dev.txt
#   开发:    pip install -r requirements.txt -r requirements-dev.txt -r requirements-dev.txt
# =============================================================================

# =============================================================================
# 基础依赖 (必需)
# =============================================================================
numpy>=1.20.0
pillow>=9.0.0

# =============================================================================
# 完整依赖 (可选，强烈推荐)
# =============================================================================
# 性能优化: 显著加速 LZSS 压缩/解压缩 (10-100x)
numba>=0.56.0

# 快速哈希: 加速哈希计算和暴力搜索
xxhash>=3.0.0

# 进度条: CLI 进度显示
tqdm>=4.64.0

# PSD 导出: 支持导出 Adobe Photoshop PSD 文件
psd-tools>=1.9.12
